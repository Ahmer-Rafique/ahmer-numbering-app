import SectionCounter from "@/components/SectionCounter"
import Header from "@/components/types/Header"

const Home = () => {
  return (
    <div>
     <Header/>
      <SectionCounter />
    </div>
  )
}

export default Home